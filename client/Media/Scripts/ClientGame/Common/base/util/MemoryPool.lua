---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Jimmy.
--- DateTime: 2019/9/2 0002 14:09
---
local Pool = {}

local function clearTable(table)
    for key, value in pairs(table) do
        if type(value) ~= "function" then
            table[key] = nil
        end
    end
end

MemoryPool = {}

function MemoryPool.newTable()
    local new = {}
    table.insert(Pool, new)
    return new
end

function MemoryPool.pushTable(pushed)
    table.insert(Pool, pushed)
end

function MemoryPool.clearPool()
    for _, table in pairs(Pool) do
        clearTable(table)
    end
    Pool = {}
end

return MemoryPool