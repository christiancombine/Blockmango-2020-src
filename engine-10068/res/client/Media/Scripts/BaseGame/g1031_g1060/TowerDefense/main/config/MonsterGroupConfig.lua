---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Jimmy.
--- DateTime: 2018/10/23 0023 11:32
---
MonsterGroupConfig = {}
MonsterGroupConfig.Config = {}

function MonsterGroupConfig:init(config)
    for _, v in pairs(config) do
        local item = {}
        item.groupId = tonumber(v.groupId)
        item.monsterId = tonumber(v.monsterId)
        item.count = tonumber(v.count)
        item.monsterDelay = tonumber(v.monsterDelay)
        item.monsterInterval = tonumber(v.monsterInterval)
        item.subHomeHp = tonumber(v.subHomeHp)
        item.killMoney = tonumber(v.killMoney)
        item.teamMoney = tonumber(v.teamMoney)
        self.Config[item.groupId] = self.Config[item.groupId] or {}
        table.insert(self.Config[item.groupId], item)
    end
end

function MonsterGroupConfig:getConfigsById(groupId)
    return self.Config[tonumber(groupId)] or {}
end

MonsterGroupConfig:init(FileUtil.getConfigFromCsv("setting/MonsterGroup.csv"))